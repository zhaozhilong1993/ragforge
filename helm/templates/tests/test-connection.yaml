apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ragforge.fullname" . }}-test-connection"
  labels:
    {{- include "ragforge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command:
      - 'wget'
      args:
      - {{ printf "%s.%s.svc" (include "ragforge.fullname" .) .Release.Namespace }}
  restartPolicy: Never
